ctrl_interface=DIR=/var/run/wpa_supplicant GROUP=netdev
update_config=1
country=US

# Red WiFi principal (reemplaza con tus datos)
network={
    ssid="TU_WIFI_PRINCIPAL"
    psk="tu_contraseña_principal"
    priority=1
}

# Red WiFi secundaria (backup)
network={
    ssid="TU_WIFI_SECUNDARIO"
    psk="tu_contraseña_secundario"
    priority=2
}

# Red WiFi de invitados
network={
    ssid="GUEST_WIFI"
    psk="contraseña_invitados"
    priority=3
}

# Red abierta (sin contraseña)
network={
    ssid="RED_ABIERTA"
    key_mgmt=NONE
    priority=4
}